//: Playground - noun: a place where people can play

import UIKit

/*:
- BOOL值
    - OC中对BOOL值得定义是typeDefine 0和1, 即非0即真, 而Swift才是真正定义了BOOL值, BOOL(true/false)
    - Swift中, 判断语句中的条件只能是BOOL值, 不能像OC一样非0即真, 这也是Swift和OC分支的最主要区别
- 细节: 
    - Swift中条件表达式的()可以省略, 但{}不可以省略, 哪怕后面只有一条执行语句, 否则报错
*/